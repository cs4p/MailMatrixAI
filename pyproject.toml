[project]
name = "mailmatrixai"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "python-dotenv>=1.0",
    "anthropic>=0.50.0",
    "flask>=3.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "httpx>=0.27"]

[tool.pytest.ini_options]
testpaths = ["tests"]
